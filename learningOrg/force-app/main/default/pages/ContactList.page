<apex:page standardController="Contact" recordSetVar="contacts">
  <apex:form >

    <apex:pageBlock title="Contact List" id="contacts_list">
      <!-- Add Filter   -->
      Filter:
      <apex:selectList value="{!filterId}" size="1">
        <apex:selectOptions value="{!listViewOptions}"></apex:selectOptions>
        <apex:actionSupport event="onchange" reRender="contact_list"></apex:actionSupport>
      </apex:selectList>
      <apex:pageBlockTable value="{!contacts}" var="ct">
        <apex:column value="{!ct.FirstName}"></apex:column>
        <apex:column value="{!ct.LastName}"></apex:column>
        <apex:column value="{!ct.Email}"></apex:column>
        <apex:column value="{!ct.Account.Name}"></apex:column>
        <apex:column value="{!ct.Owner.Name}"></apex:column>
      </apex:pageBlockTable>
    </apex:pageBlock>
  </apex:form>

</apex:page>